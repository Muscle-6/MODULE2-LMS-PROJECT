spring:
  datasource:
    url: jdbc:mysql://localhost:3306/module02_lms
    username: Alien
    password: Alien

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  thymeleaf:
    cache: false

toss:
  client-key: test_ck_placeholder_여기에_클라이언트키_입력
  secret-key: test_sk_placeholder_여기에_시크릿키_입력

server:
  servlet:
    session:
      timeout: 24h
      cookie:
        http-only: true
        secure: false
        same-site: lax